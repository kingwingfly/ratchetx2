E2EE chat TUI client.

```sh
An E2EE chat client.

Usage: e2ee_chat_client --server-addr <SERVER_ADDR>

Options:
  -s, --server-addr <SERVER_ADDR>  The E2EE chat server address
  -h, --help                       Print help
  -V, --version                    Print version
```
